#include <stdio.h>

/**
 * main - prints phrase
 * Return:0 - always returns to 0 at success 
 */
int main(void)
{
char output[] = "\"Programming is like building a multilingual puzzle";
puts(output);
return (0);
}
